<?php
/**
 * Auto-selected by WordPress for the Czech Magazín page (slug "magazin",
 * /cz/magazin/) via the page-{slug}.php convention. The Czech Journal is
 * implemented entirely inside page-blog.php (it branches on
 * pll_current_language()); this file exists because the CZ page's slug
 * can't be "blog" — that slug belongs to EN page 881 and sharing one slug
 * across languages needs Polylang Pro — and a REST-assigned
 * _wp_page_template meta proved unreliable on prod. Slug-convention
 * selection needs no meta at all, same as page-foot-covers.php etc.
 */

require __DIR__ . '/page-blog.php';
